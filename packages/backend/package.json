{
  "name": "@checkmate/backend",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env run --watch src/index.ts",
    "generate": "bun --env-file=../../.env run drizzle-kit generate:pg",
    "seed": "bun --env-file=../../.env run src/seed.ts"
  },
  "dependencies": {
    "@checkmate/core-api": "workspace:*",
    "better-auth": "^1.4.7",
    "drizzle-orm": "^0.30.0",
    "hono": "^4.0.0",
    "jose": "^6.1.3",
    "pg": "^8.11.0",
    "winston": "^3.19.0",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.20.0",
    "@types/pg": "^8.11.0",
    "@types/bun": "latest"
  }
}
