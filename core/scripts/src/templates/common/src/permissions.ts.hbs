import { createPermission } from "@checkmate-monitor/common"; export const
permissions = {
{{pluginNameCamel}}Read: createPermission( "{{pluginBaseName}}", "read", "Read
{{pluginBaseName}}
data", { isAuthenticatedDefault: true } // Auto-assigned to "users" role ),
{{pluginNameCamel}}Manage: createPermission( "{{pluginBaseName}}", "manage",
"Manage
{{pluginBaseName}}
data" ), }; export const permissionList = Object.values(permissions);