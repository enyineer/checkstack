import { createRoutes } from "@checkmate-monitor/common"; /** * Route
definitions for the
{{pluginBaseName}}
plugin. * Use these for type-safe route generation with resolveRoute(). */
export const
{{pluginNameCamel}}Routes = createRoutes("{{pluginBaseName}}", { home: "/", });