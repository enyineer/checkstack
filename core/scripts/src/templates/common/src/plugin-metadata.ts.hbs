import { definePluginMetadata } from "@checkmate/common";
/**
 * Plugin metadata for the {{pluginNamePascal}} plugin.
 * Exported from the common package so both backend and frontend can
 * reference it.
 */
export const pluginMetadata = definePluginMetadata({ pluginId: "{{pluginId}}", });