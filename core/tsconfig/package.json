{
  "name": "@checkmate-monitor/tsconfig",
  "version": "1.0.0",
  "private": true,
  "files": [
    "base.json",
    "frontend.json",
    "backend.json",
    "common.json"
  ],
  "scripts": {
    "typecheck": "echo skipping",
    "lint": "echo skipping",
    "lint:code": "eslint . --max-warnings 0"
  },
  "devDependencies": {
    "@checkmate-monitor/scripts": "workspace:*"
  }
}
