---
"@checkstack/ai-backend": patch
---

Regenerate the docs search index to include the new "Monitor containers" guide
and the container health-check documentation, so the AI assistant can surface
the secure socket-proxy setup and the Container strategy/collectors.
