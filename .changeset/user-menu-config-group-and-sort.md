---
"@checkstack/script-packages-frontend": patch
"@checkstack/secrets-frontend": patch
"@checkstack/auth-frontend": patch
---

Tidy the user menu: move "Script packages" and "Secrets" into the **Configuration** group (the now-empty **Administration** group is gone), and display the user-menu groups in alphabetical order instead of a hardcoded canonical order.
