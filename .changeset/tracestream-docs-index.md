---
"@checkstack/ai-backend": patch
---

Regenerate the docs index to include the new "Trace streams" developer-guide
page (OTLP ingestion, tail-based sampling, storage tiers, query API and the
traces telemetry sink).
