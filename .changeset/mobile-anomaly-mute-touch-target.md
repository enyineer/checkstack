---
"@checkstack/anomaly-frontend": patch
---

Enlarge the per-field mute/unmute icon button in the anomaly field mute list to a
40px touch target (`h-10 w-10`) so it is reliably tappable on touch devices.
