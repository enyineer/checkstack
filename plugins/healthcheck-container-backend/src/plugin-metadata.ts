import { definePluginMetadata } from "@checkstack/common";

/**
 * Plugin metadata for the Container Health Check backend.
 */
export const pluginMetadata = definePluginMetadata({
  pluginId: "healthcheck-container",
});
